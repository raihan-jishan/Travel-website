import style from '../Loginpage.module.css';

const Title = () => {
  return (
    <div>
        <h2 className={style.LoginTitle}>Fell free contact or Login our page </h2>
    </div>
  )
}

export default Title;